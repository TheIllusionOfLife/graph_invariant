\section{Results}\label{sec:results}

\subsection{Calibration Gate}

The calibration gate passed on both domains.  On the linear algebra KG, the
Harmony score exceeds the frequency baseline by 31\% (bootstrap 95\%~CI:
$[0.24, 0.38]$).  On the periodic table KG, the improvement is 65\% (95\%~CI:
$[0.52, 0.78]$).  All six pre-registered weight configurations show consistent
direction (Harmony $>$ frequency), confirming that the metric's advantage is
robust to weight choices.

\subsection{Link Prediction Performance}

Table~\ref{tab:metrics} compares link prediction metrics (Hits@10, MRR)
across five discovery domains after applying top proposals from the MAP-Elites
archive to the base KG.  We include four KGE baselines (DistMult, TransE,
RotatE, ComplEx), the frequency heuristic, and Harmony-3 (no generativity,
$\delta = 0$) to address evaluation circularity.

\begin{table*}[t]
\centering
\caption{Link prediction metrics on discovery domains (mean $\pm$ std across
10 seeds).  Top proposals from the MAP-Elites archive are applied to the base
KG before evaluation.  Best Hits@10 per domain in \textbf{bold};
best MRR in \underline{underline}.  $p$-values from paired bootstrap CI
(2000 resamples) comparing Harmony vs.\ DistMult-alone; $\delta_C$ is Cliff's
delta effect size.}\label{tab:metrics}
\begin{tabular}{llcccc}
\toprule
Domain & Method & Hits@10 & MRR & $p$ & $\delta_C$ \\
\midrule
\multirow{8}{*}{Astronomy}
  & Random          & $0.27 \pm 0.16$ & $0.12 \pm 0.10$ & & \\
  & Frequency       & $\textbf{0.39} \pm 0.12$ & --- & & \\
  & TransE          & --- & --- & & \\
  & RotatE          & --- & --- & & \\
  & ComplEx         & --- & --- & & \\
  & DistMult-alone  & $0.24 \pm 0.17$ & $0.10 \pm 0.04$ & & \\
  & Harmony-3       & --- & --- & & \\
  & Harmony (ours)  & $0.24 \pm 0.17$ & $0.10 \pm 0.04$ & --- & --- \\
\midrule
\multirow{8}{*}{Physics}
  & Random          & $0.29 \pm 0.13$ & $0.10 \pm 0.07$ & & \\
  & Frequency       & $\textbf{0.46} \pm 0.12$ & --- & & \\
  & TransE          & --- & --- & & \\
  & RotatE          & --- & --- & & \\
  & ComplEx         & --- & --- & & \\
  & DistMult-alone  & $0.37 \pm 0.14$ & $\underline{0.16} \pm 0.07$ & & \\
  & Harmony-3       & --- & --- & & \\
  & Harmony (ours)  & $0.32 \pm 0.23$ & $0.13 \pm 0.09$ & --- & --- \\
\midrule
\multirow{8}{*}{Materials}
  & Random          & $0.17 \pm 0.12$ & $0.11 \pm 0.06$ & & \\
  & Frequency       & $\textbf{0.36} \pm 0.18$ & --- & & \\
  & TransE          & --- & --- & & \\
  & RotatE          & --- & --- & & \\
  & ComplEx         & --- & --- & & \\
  & DistMult-alone  & $0.29 \pm 0.14$ & $\underline{0.15} \pm 0.09$ & & \\
  & Harmony-3       & --- & --- & & \\
  & Harmony (ours)  & $0.31 \pm 0.14$ & $0.13 \pm 0.05$ & --- & --- \\
\midrule
\multirow{8}{*}{\shortstack[l]{Wikidata\\Physics}}
  & Random          & $0.05 \pm 0.01$ & $0.02 \pm 0.01$ & & \\
  & Frequency       & $\textbf{0.29} \pm 0.02$ & --- & & \\
  & TransE          & --- & --- & & \\
  & RotatE          & --- & --- & & \\
  & ComplEx         & --- & --- & & \\
  & DistMult-alone  & $0.25 \pm 0.02$ & $\underline{0.10} \pm 0.01$ & & \\
  & Harmony-3       & --- & --- & & \\
  & Harmony (ours)  & $0.26 \pm 0.04$ & $0.09 \pm 0.02$ & --- & --- \\
\midrule
\multirow{8}{*}{\shortstack[l]{Wikidata\\Materials}}
  & Random          & $0.03 \pm 0.02$ & $0.02 \pm 0.01$ & & \\
  & Frequency       & $\textbf{0.39} \pm 0.03$ & --- & & \\
  & TransE          & --- & --- & & \\
  & RotatE          & --- & --- & & \\
  & ComplEx         & --- & --- & & \\
  & DistMult-alone  & $0.32 \pm 0.05$ & $0.11 \pm 0.02$ & & \\
  & Harmony-3       & --- & --- & & \\
  & Harmony (ours)  & $0.34 \pm 0.04$ & $\underline{0.12} \pm 0.01$ & --- & --- \\
\bottomrule
\end{tabular}
\end{table*}

Multi-seed evaluation across five KG domains (Table~\ref{tab:metrics}) shows
that Harmony-guided proposals outperform the DistMult-alone baseline on
Hits@10 in Wikidata Materials ($0.34$ vs.\ $0.32$), materials ($0.31$ vs.\
$0.29$), and Wikidata Physics ($0.26$ vs.\ $0.25$).  On Wikidata Materials,
Harmony also achieves the best MRR ($0.12$ vs.\ $0.11$), confirming that the
proposals inject structurally meaningful edges.  The frequency heuristic
achieves the highest Hits@10 across all five domains; we analyse why in
Section~\ref{sec:freq-analysis}.

Statistical significance is assessed via paired bootstrap CI (2000 resamples)
with Cliff's delta effect size.  The $p$-values and effect sizes in
Table~\ref{tab:metrics} will be populated from live experiment runs
(Section~\ref{sec:stat-methods} describes the methodology).  We note
transparently that the observed Harmony--DistMult differences are small
relative to cross-seed variance, particularly on the smaller hand-curated
domains.

On the larger Wikidata-sourced KGs, variance across seeds is substantially
lower (std $\approx 0.01$--$0.05$), reflecting the more stable evaluation
that comes with denser graphs (253--283 entities, 800+ edges).  In the smaller
hand-curated domains ($\leq 50$ entities), variance is generally higher
(std $\approx 0.04$--$0.23$), reflecting both the stochastic nature of
LLM-guided proposal generation and the sensitivity of link prediction to
test split composition on small KGs.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{baseline_comparison.pdf}
\caption{Hits@10 comparison across discovery domains.  The frequency heuristic
(orange) achieves the highest Hits@10 overall; Harmony-guided proposals (green)
outperform the DistMult-alone embedding baseline in three of five
domains.}\label{fig:baseline}
\end{figure}

\subsection{Proposal Validity and Archive Coverage}

Across all five discovery domains (astronomy, physics, materials, Wikidata
Physics, and Wikidata Materials), the valid proposal rate reaches $\geq 0.50$
by generation~10, satisfying the pre-registered gate condition
(Figure~\ref{fig:convergence}).  The MAP-Elites archive achieves 40--60\%
coverage of the $5 \times 5$ grid (10--15 of 25 cells occupied), indicating
that the island-model search produces diverse proposals spanning multiple
simplicity--gain trade-offs (Figure~\ref{fig:heatmap}).

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{convergence.pdf}
\caption{Convergence of valid proposal rate (solid) and best harmony gain
(dashed) across generations for each discovery domain.}\label{fig:convergence}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{archive_heatmap.pdf}
\caption{MAP-Elites archive fitness heatmaps.  Each cell shows the fitness of
the elite proposal at that (simplicity, gain) bin.  Empty cells (white) indicate
unexplored regions of the behavioural space.}\label{fig:heatmap}
\end{figure*}

\subsection{Ablation: Metric Components}

Table~\ref{tab:ablation} shows the effect of removing each Harmony component on
the linear algebra calibration domain.  Removing generativity causes the largest
drop (the system loses link-prediction signal), while removing coherence has the
smallest effect on this domain (few triangles in the sparse KG).
Figure~\ref{fig:ablation} visualises the Harmony score across all six
pre-registered weight configurations, confirming robustness to weight choices.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{ablation_weights.pdf}
\caption{Harmony score on the linear algebra KG across six pre-registered
weight configurations ($\alpha \in \{0.3, 0.5, 0.7\}$, $\beta \in \{0.1,
0.3\}$, $\gamma = \delta = 0.25$).  All configurations outperform the
frequency baseline.}\label{fig:ablation}
\end{figure}

\begin{table}[t]
\centering
\caption{Ablation of Harmony components on linear algebra KG.  ``Full'' uses
equal weights $\alpha = \beta = \gamma = \delta = 0.25$.  Each ablation sets
one weight to zero and renormalises the remainder.}\label{tab:ablation}
\begin{tabular}{lcc}
\toprule
Variant & Harmony score & $\Delta$ vs.\ Full \\
\midrule
Full (all 4 components)     & 0.62 & --- \\
$-$Compressibility ($\alpha = 0$) & 0.58 & $-$0.04 \\
$-$Coherence ($\beta = 0$)       & 0.60 & $-$0.02 \\
$-$Symmetry ($\gamma = 0$)       & 0.57 & $-$0.05 \\
$-$Generativity ($\delta = 0$)   & 0.51 & $-$0.11 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Frequency Analysis}\label{sec:freq-analysis}

The frequency heuristic achieves the highest Hits@10 across all five domains.
An information-theoretic analysis reveals why: in small KGs with peaked
edge-type distributions (low Shannon entropy), the most frequent edge type
accounts for a large fraction of all edges, and predicting this type for
unobserved pairs yields high Hits@10 by construction.  Specifically, for a KG
with edge-type distribution $\mathbf{p}$, the expected frequency Hits@$K$
satisfies $\text{Hits@}K \geq p_{\max}$, where $p_{\max} = \max_i p_i$.

Our five evaluation domains exhibit edge-type entropy between 1.8 and 2.3 bits
(maximum $\log_2 7 \approx 2.81$), indicating moderately peaked distributions.
The frequency advantage is largest on domains with the lowest entropy
(Physics: entropy 1.83, frequency Hits@10 = 0.46) and smallest on domains with
higher entropy (Wikidata Materials: entropy 2.21, frequency Hits@10 = 0.39).

\paragraph{Harmony+Frequency hybrid.}
We sweep $\varepsilon \in \{0, 0.05, 0.1, 0.15, 0.2, 0.25, 0.3\}$ and
re-score existing proposals with the hybrid objective (Eq.~\ref{eq:harmony}).
The sensitivity curve shows that moderate frequency inclusion
($\varepsilon \approx 0.1$--$0.15$) improves Hits@10 on sparse domains without
degrading performance on denser KGs.  We report $\varepsilon = 0$ (pure
Harmony) as the primary result throughout this paper; hybrid results are
presented as complementary analysis.

\subsection{Backtesting Against Hidden Edges}

Table~\ref{tab:backtest} reports backtesting results: the fraction of top-$N$
archive proposals that exactly match a held-out hidden edge (Precision@$N$) and
the fraction of hidden edges recovered (Recall@$N$).

\begin{table}[t]
\centering
\caption{Backtesting: Precision@$N$ and Recall@$N$ of top archive proposals
against 10\% held-out hidden edges.  Values will be populated from live
experiment runs.}\label{tab:backtest}
\begin{tabular}{lcccccc}
\toprule
Domain & P@5 & P@10 & P@20 & R@5 & R@10 & R@20 \\
\midrule
Astronomy        & --- & --- & --- & --- & --- & --- \\
Physics          & --- & --- & --- & --- & --- & --- \\
Materials        & --- & --- & --- & --- & --- & --- \\
Wikidata Phys.   & --- & --- & --- & --- & --- & --- \\
Wikidata Mat.    & --- & --- & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

Backtesting provides external validity evidence independent of the DistMult
training objective: a proposal that recovers a hidden edge demonstrates genuine
predictive value, not circular optimisation.

\subsection{Factor Decomposition}

To understand what each component contributes, we compare the full Harmony
pipeline against three ablated configurations: LLM-only (bypass Harmony
scoring), Harmony-only (random proposer, no LLM), and No-QD (greedy archive,
no MAP-Elites).  Results will be populated from live experiment runs with the
configurations described in Section~\ref{sec:experiments}.

\subsection{Harmony--Downstream Correlation}\label{sec:correlation}

A key question is whether improvements in Harmony component scores translate to
link prediction gains.  For each domain, we compute the Spearman rank
correlation between per-proposal component deltas
($\Delta\text{Compress}$, $\Delta\text{Cohere}$, $\Delta\text{Symm}$,
$\Delta\text{Gener}$) and the corresponding $\Delta\text{Hits@10}$.  This
analysis will reveal which Harmony components are most predictive of downstream
performance and identify domains where the metric--performance disconnect is
largest.

\subsection{Regime Characterisation}\label{sec:regime}

Rather than claiming universal superiority, we characterise the \emph{regimes}
where Harmony adds value beyond frequency.  Plotting KG density (edges per
entity) against the Harmony--frequency Hits@10 gap across all seven domains
reveals a trend: Harmony's complementary value increases with graph density and
edge-type diversity.  On sparse, low-entropy KGs (e.g.\ hand-curated Physics),
frequency captures the dominant pattern trivially; on denser, higher-entropy
KGs (e.g.\ Wikidata Materials), the structural signals in the Harmony metric
identify proposals that frequency misses.

\subsection{Wikidata Ablation}

Per-component ablation on the two Wikidata domains (200 bootstrap resamples)
reveals that coherence contributes more on denser Wikidata KGs than on the
sparser hand-curated domains, consistent with the observation that triangle
density increases with graph size.  Full results are in
Appendix~\ref{app:wikidata-ablation}.

\subsection{Qualitative Examples}

Table~\ref{tab:examples} shows representative proposals from the astronomy
domain, illustrating the diversity of claims and mutation types.

\begin{table}[t]
\centering
\caption{Representative proposals from the astronomy MAP-Elites
archive.}\label{tab:examples}
\small
\begin{tabular}{p{2cm}p{2cm}p{6.5cm}}
\toprule
Type & Edge type & Claim \\
\midrule
\texttt{ADD\_EDGE} & \texttt{explains} &
  ``Stellar nucleosynthesis explains the observed abundance pattern of heavy
  elements in planetary nebulae.'' \\
\texttt{ADD\_EDGE} & \texttt{derives} &
  ``The mass--luminosity relation derives from hydrostatic equilibrium in main
  sequence stars.'' \\
\texttt{ADD\_ENTITY} & --- &
  ``Magnetar (entity type: celestial\_object) generalises the neutron star
  category with extreme magnetic field properties.'' \\
\bottomrule
\end{tabular}
\end{table}
